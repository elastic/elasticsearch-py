// query-dsl/multi-match-query.asciidoc:11

[source, python]
----
resp = client.search(
    body={
        "query": {
            "multi_match": {
                "query": "this is a test",
                "fields": ["subject", "message"],
            }
        }
    },
)
print(resp)
----