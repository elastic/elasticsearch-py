// query-dsl/multi-match-query.asciidoc:50

[source, python]
----
resp = client.search(
    body={
        "query": {
            "multi_match": {
                "query": "this is a test",
                "fields": ["subject^3", "message"],
            }
        }
    },
)
print(resp)
----