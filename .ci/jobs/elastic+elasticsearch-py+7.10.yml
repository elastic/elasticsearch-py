---
- job:
    name: elastic+elasticsearch-py+7.10
    display-name: 'elastic / elasticsearch-py # 7.10'
    description: Testing the elasticsearch-py 7.10 branch.
    junit_results: "junit/*-junit.xml"
    parameters:
    - string:
        name: branch_specifier
        default: refs/heads/7.10
        description: the Git branch specifier to build (&lt;branchName&gt;, &lt;tagName&gt;,
          &lt;commitId&gt;, etc.)
